import UIComponent from "sap/ui/core/UIComponent";

/**
 * @namespace btp.drift.destinationdrift
 */
export default class Component extends UIComponent {
  static metadata = {
    manifest: "json",
  };

  init(): void {
    super.init();
  }
}
